import { InCompletedTaskPipe } from './in-completed-task.pipe';

describe('InCompletedTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new InCompletedTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
