import { CompletedTasksPipePipe } from './completed-tasks-pipe.pipe';

describe('CompletedTasksPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedTasksPipePipe();
    expect(pipe).toBeTruthy();
  });
});
